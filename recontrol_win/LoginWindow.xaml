<Window x:Class="recontrol_win.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Login" Height="400" Width="320"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#FAFAFA">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Log In"
                   FontSize="24"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,20,0,30"/>

        <!-- Form Fields -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="0,0,0,40">
            <TextBlock Text="Email" Margin="5,0,0,3" FontWeight="SemiBold"/>
            <TextBox x:Name="EmailTextBox"
                     Height="30"
                     Padding="5"
                     Margin="0,0,0,10"/>

            <TextBlock Text="Password" Margin="5,0,0,3" FontWeight="SemiBold"/>
            <PasswordBox x:Name="PasswordBox"
                         Height="30"
                         Padding="5"
                         Margin="0,0,0,20"/>

            <Button x:Name="LoginButton"
                    Content="Log In"
                    Height="35"
                    Background="#0078D7"
                    Foreground="White"
                    FontWeight="Bold"
                    BorderThickness="0"
                    Cursor="Hand"/>
        </StackPanel>

        <!-- Signup link -->
        <StackPanel Grid.Row="2" HorizontalAlignment="Center" Orientation="Horizontal">
            <TextBlock Text="Don't have an account? " />
            <TextBlock>
                <Hyperlink Click="SignUpLink_Click" Foreground="#0078D7" Cursor="Hand">
                    Sign up
                </Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>
