<Window x:Class="recontrol_win.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Recontrol" Height="400" Width="600"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" VerticalAlignment="Center" Margin="0,0,0,10">
            <TextBlock Text="WebSocket Status: " VerticalAlignment="Center"/>
            <TextBlock x:Name="StatusText" Text="Disconnected" FontWeight="Bold" Margin="5,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10" VerticalAlignment="Center">
            <Button x:Name="SendPingButton" Content="Send Ping" Margin="0,0,0,0" Click="SendPingButton_Click" IsEnabled="False"/>
        </StackPanel>

        <GroupBox Header="Messages" Grid.Row="2">
            <ScrollViewer>
                <StackPanel x:Name="MessagesPanel"/>
            </ScrollViewer>
        </GroupBox>
    </Grid>
</Window>
